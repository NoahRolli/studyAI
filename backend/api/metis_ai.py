# Metis AI — Legacy (ersetzt durch /api/concepts/auto-link + auto-cluster)
# Datei bleibt für Import-Kompatibilität, Endpoints sind leer

from fastapi import APIRouter
router = APIRouter(prefix="/api/metis", tags=["metis-ai"])
